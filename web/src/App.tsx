/**
 * Phase 0 placeholder shell — replaced by the full chat experience in Phase 5.
 * Kept intentionally minimal but themed, to prove the token pipeline renders.
 */
export default function App() {
  return (
    <main
      style={{
        minHeight: "100%",
        display: "grid",
        placeItems: "center",
        background: "var(--bg-glow)",
        fontFamily: "var(--font-display)",
        color: "var(--text-ink)",
      }}
    >
      <p style={{ opacity: 0.7 }}>Lakeside Dental — coming online…</p>
    </main>
  );
}
